<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 해당 파일에 모든 쿼리문을 작성 -->
<!-- namespace에는 xml파일과 연결할 Mapper interface 파일명을 패키지를 포함하여 적어주세요 -->
<mapper namespace="com.green.car_sales.car.mapper.CarMapper">
	<resultMap id="car" type="com.green.car_sales.car.dto.CarDTO">
		<id 	column="MODEL_NUMBER" 			property="modelNumber" />
		<result column="MODEL_NAME" 	property="modelName" />
		<result column="MODEL_PRICE" 	property="modelPrice" />
		<result column="MANUFACTURER" 	property="manufacturer" />
	</resultMap>

	<!-- 차량 등록 쿼리-->
	<insert id="insertCar">
		INSERT INTO CAR_INFO (
			MODEL_NAME
			, MODEL_PRICE
			, MANUFACTURER
		) VALUES (
			#{modelName}
			, #{modelPrice}
			, #{manufacturer}
		)
	</insert>


	<!--등록된 차량 리스트 조회 쿼리-->
	<select id="selectCarList" resultMap="car">
		SELECT MODEL_NUMBER
			, MODEL_NAME
			, MODEL_PRICE
			, MANUFACTURER
		FROM CAR_INFO
		ORDER BY MODEL_NUMBER DESC
	</select>


</mapper>































